// Layout wrapper — not used by the new Dashboard page, kept for backwards compatibility.
// The main dashboard layout is composed directly in pages/Dashboard.jsx.
export { default } from './Sidebar';